@model IEnumerable<RecordManagement.Models.Student>

    <link rel="stylesheet" href="~/Content/Site.css" />

@{
    ViewBag.Title = "Index";
 
}

<h2>Index</h2>


<p>
    @Html.ActionLink("Create New", "Create", new {@style="color:black;"})
</p>
<p>
    @using (Html.BeginForm("Index", "Students", FormMethod.Get))
    {
        <b>Search by Last Name</b>
        @Html.TextBox("search")
        <input type="submit" value="Search" />
    }
</p>
@*<p>
    @using (Html.BeginForm("Index", "StudentsController", FormMethod.Get))
    {
    <b>Search by Name</b>
    @Html.TextBox("search") <input type="submit" />
    }
</p>*@

<div id="google_translate_element"></div>
<script type="text/javascript">
function googleTranslateElementInit() {
  new google.translate.TranslateElement({pageLanguage: 'en', layout: google.translate.TranslateElement.InlineLayout.SIMPLE}, 'google_translate_element');
}
</script>
<script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>

@{ 
if (TempData["message"] != null) {
<script type="text/javascript">
        alert(@Html.Raw(Json.Encode(TempData["message"])));
</script>
                }
                }

<table class="table table-condensed table-hover table-bordered">
    <tr class="success">
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstMidName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EnrollmentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstMidName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EnrollmentDate)
        </td>
        <td>
            <button  class="btn btn-xs btn-default">  @Html.ActionLink("Edit", "Edit", new { id = item.ID })  </button>
            <button class="btn btn-xs btn-default"> @Html.ActionLink("Details", "Details", new { id = item.ID }) </button>
            <button class="btn btn-xs btn-default">
                @Html.ActionLink("Delete", "Delete", new { id = item.ID })
            </button>
        </td>
    </tr>
}

</table>
